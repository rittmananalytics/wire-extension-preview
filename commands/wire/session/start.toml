description = "DEPRECATED — context loading is now automatic via the engagement-context skill; use /wire:plan for optional structured planning"
prompt = """
# DEPRECATED — context loading is now automatic via the engagement-context skill; use /wire:plan for optional structured planning

## User Input ((optional: release-folder))

{{args}}

## Path Configuration

- **Projects**: `.wire` (project data and status files)

When following the workflow specification below, resolve paths as follows:
- `.wire/` in specs refers to the `.wire/` directory in the current repository
- `TEMPLATES/` references refer to the templates section embedded at the end of this command

## Telemetry

# Telemetry — Anonymous Usage Tracking

## Purpose

Send one anonymous usage event so the Wire Framework team can understand adoption and usage patterns.

> **Runtime note.** On the **Claude Code plugin**, telemetry is sent by a `UserPromptExpansion` hook (`hooks/wire-telemetry.sh`) that fires when a `/wire:` slash command is run — it does not appear in the console as a tool call. This in-command version is used only by the **Gemini CLI extension**, which has no hook system. The build script injects it into Gemini commands and skips it for Claude commands.

## Send the event

Run the following as a **single** Bash command, exactly as written. It:

- skips everything if `WIRE_TELEMETRY=false`,
- creates an anonymous id on first run (and sends an `identify` call),
- sends the `track` event,
- backgrounds the whole thing (`&`) with all output suppressed (`> /dev/null 2>&1`).

Do not split it, do not inspect its output, and proceed immediately to the Workflow Specification. If any part fails (no network, no `curl`, no `python3`), it silently does nothing — telemetry must never block or fail the workflow.

```bash
[ "${WIRE_TELEMETRY:-true}" = "false" ] || ( mkdir -p ~/.wire; if [ ! -f ~/.wire/telemetry_id ]; then WIRE_UID=$(python3 -c "import uuid; print(uuid.uuid4())" 2>/dev/null || uuidgen | tr '[:upper:]' '[:lower:]'); echo "$WIRE_UID" > ~/.wire/telemetry_id; curl -s -X POST https://api.segment.io/v1/identify -H "Content-Type: application/json" -d "{\"writeKey\":\"DxXwrT6ucDMRmouCsYDwthdChwDLsNYL\",\"userId\":\"$WIRE_UID\",\"traits\":{\"username\":\"$(whoami)\",\"hostname\":\"$(hostname)\",\"os\":\"$(uname -s)\",\"plugin_version\":\"4.0.0-preview\",\"first_seen\":\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"}}" > /dev/null 2>&1; fi; WIRE_UID=$(cat ~/.wire/telemetry_id 2>/dev/null || echo unknown); curl -s -X POST https://api.segment.io/v1/track -H "Content-Type: application/json" -d "{\"writeKey\":\"DxXwrT6ucDMRmouCsYDwthdChwDLsNYL\",\"userId\":\"$WIRE_UID\",\"event\":\"wire_command\",\"properties\":{\"command\":\"session-start\",\"timestamp\":\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\",\"git_repo\":\"$(git config --get remote.origin.url 2>/dev/null || echo unknown)\",\"git_branch\":\"$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo unknown)\",\"username\":\"$(whoami)\",\"hostname\":\"$(hostname)\",\"plugin_version\":\"4.0.0-preview\",\"os\":\"$(uname -s)\",\"runtime\":\"gemini\",\"autopilot\":\"false\"}}" > /dev/null 2>&1 ) > /dev/null 2>&1 &
```

## Rules

1. **Never block** — runs in the background with all output suppressed.
2. **Never fail the workflow** — if telemetry fails for any reason, silently continue.
3. **Execute as a single Bash command** — do not split it.
4. **Do not inspect the result** — fire and forget.
5. **Proceed immediately** — continue to the Workflow Specification without waiting.

## Workflow Specification

---
description: "DEPRECATED — session:start has been replaced by the engagement-context skill and /wire:plan"
deprecated: true
replaced_by: "engagement-context skill (auto-fires) + /wire:plan (optional planning ritual)"
since: "3.4.20"
---

# ⚠️ Deprecated: /wire:session:start

This command has been deprecated in Wire v3.4.20.

## Why it was removed

Telemetry analysis across six Wire engagements showed that session:start was rarely run consistently — consultants would begin work without invoking it, losing the planning benefit it was designed to provide. The root problem is that placing session lifecycle management in explicit commands puts the burden on the user to remember to run them. In practice, people just start working.

## What replaces it

**Context loading** is now handled by the **engagement-context skill**, which activates automatically whenever Claude detects a `.wire/` directory and has not yet established engagement context in the current conversation. No invocation required.

**Structured session planning** is available on demand via `/wire:plan`, for consultants who want the 3–5 step session plan before starting work. This is optional, not mandatory.

## Migration

- Remove any `/wire:session:start` invocations from your workflow
- Context will be loaded automatically when you begin work in a Wire engagement
- Run `/wire:plan` at the start of a session if you want an explicit plan before proceeding

Execute the complete workflow as specified above.
"""
